spring:
  application:
    name: inscritos
  datasource:
    url: jdbc:postgresql://localhost:5432/desafiodb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true